{
  "name": "web1",
  "version": "1.0.0",
  "description": "This webserver will provide the user the ability to encode messages with a shift cipher.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Raghu",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}